export class DeadlineResult
{
    constructor(unixDeadline,stringDeadline,time)
    {
        this.unixDeadline = unixDeadline;
        this.stringDeadline = stringDeadline;
        this.time = time;
    }
}